import type { FC } from 'react'

// TODO [NON-BLOCKER]: Add routing (React Router v7 or TanStack Router)
// TODO [NON-BLOCKER]: Add global state provider if needed (Zustand/Context)
// TODO [NON-BLOCKER]: Add error boundary at app level
export const App: FC = () => {
  return (
    <main>
      <p>Review Page — harness ready</p>
    </main>
  )
}
